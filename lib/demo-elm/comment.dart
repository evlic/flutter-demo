import 'package:flutter/material.dart';

class ElmCommentApp extends StatelessWidget {
  const ElmCommentApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("评论"),);
  }
}
